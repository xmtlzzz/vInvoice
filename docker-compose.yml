version: '3.8'

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      # Persist data.json outside the container
      - ./server/data.json:/app/server/data.json
